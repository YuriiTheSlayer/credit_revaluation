[project]
name = "payment-terms-dashboard"
version = "0.1.0"
description = "Анализ сроков рассрочки Comfy vs конкуренты: локальный дашборд + экспорт в Excel"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
